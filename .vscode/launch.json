//run this in the terminal before launching debugger
//node --inspect-brk ./node_modules/jest/bin/jest.js --runInBand --watch
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach",
      "port": 9229
    }
  ]
}
